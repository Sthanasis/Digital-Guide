name: Tests
on: 
  push: 
  branches: [main]

jobs: 
  setup: 
    name: Installing Dependencies
    runs-on: ubuntu-latest
    steps: 
      - name: Install Node
        run: npm install
  test:
    name: Testing
    needs: setup
    runs-on: ubuntu-latest
    steps:
      run: npm test
